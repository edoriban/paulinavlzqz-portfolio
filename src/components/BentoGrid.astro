---
interface Props {
  columns?: number;
  class?: string;
}

const { columns = 12, class: className } = Astro.props;
---

<div
  class:list={[
    "grid h-fit w-full",
    columns === 12 ? "grid-cols-12" : `grid-cols-${columns}`,
    className,
  ]}
>
  <slot />
</div>
